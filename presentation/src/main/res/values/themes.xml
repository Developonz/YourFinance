<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.YourFinance" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item> <!-- ССЫЛКА на цвет в colors.xml -->
        <item name="colorPrimaryVariant">@color/purple_700</item> <!-- ССЫЛКА на цвет в colors.xml -->
        <item name="colorOnPrimary">@color/white</item> <!-- ССЫЛКА на цвет в colors.xml (или ваш) -->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item> <!-- ССЫЛКА на цвет в colors.xml -->
        <item name="colorSecondaryVariant">@color/teal_700</item> <!-- ССЫЛКА на цвет в colors.xml -->
        <item name="colorOnSecondary">@color/black</item> <!-- ССЫЛКА на цвет в colors.xml (или ваш) -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorSurfaceVariant">@color/grey_100</item>

        <!-- Эти атрибуты тоже важны -->
        <item name="colorSurface">@color/white</item> <!-- ССЫЛКА на цвет в colors.xml (или ваш) -->
        <item name="colorOnSurface">@color/black</item> <!-- ССЫЛКА на цвет в colors.xml (или ваш) -->
        <item name="android:colorBackground">@color/grey_100</item> <!-- ССЫЛКА на цвет в colors.xml (или ваш) -->
        <item name="colorError">@color/design_default_color_error</item> <!-- Можно оставить стандартный или ссылаться на свой -->
        <item name="colorOnError">@color/white</item> <!-- ССЫЛКА на цвет в colors.xml (или ваш) -->


        <item name="colorControlNormal">@color/black</item> <!-- ССЫЛКА на цвет в colors.xml (или ваш) -->
        <item name="colorControlHighlight">@color/ripple_material_light</item> <!-- ССЫЛКА на цвет в colors.xml (или ваш) -->
    </style>


</resources>